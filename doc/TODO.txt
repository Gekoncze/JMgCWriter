### NOW ###
    ...

### SOON ###
    add test for CEntityFormatters

    finish Formatter implementation + test
        make sure that semicolon is added at the end of statements

### LATER ###
    add support for directives and macro definitions
        these should be as wrapper on top of CFile
        could be either wrapper or inheritance
        only limited functionality will be provided
            ability to specify list of leading and trailing directives

    finish implementation for Writer + test

    add support for varargs